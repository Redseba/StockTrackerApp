import 'package:flutter/material.dart';

class StockDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Stock Detail')),
      body: Center(child: Text('Stock Detail Content')),
    );
  }
}
